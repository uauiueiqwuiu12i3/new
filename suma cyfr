#include <iostream>
using namespace std;

int main()
{
    int n,suma=0;

    cout << "Podaj lczb naturln: ";
    cin >> n;

    while(n>0)
    {
    if((n%10)%2!=0) suma+=(n%10);
    n/=10;
    }

    cout << "\nSuma nieprzystych cyfr tej liczby wynsi " << suma << ".\n";
    return 0;
}
